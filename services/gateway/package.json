{
  "name": "@dct/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "lint": "echo 'lint not configured for @dct/gateway'",
    "test": "echo 'no tests for @dct/gateway'",
    "format": "prettier --write 'src/**/*.ts'"
  },
  "dependencies": {
    "@dct/config": "workspace:*",
    "@dct/contracts": "workspace:*",
    "@dct/db": "workspace:*",
    "@dct/runtime": "workspace:*",
    "@fastify/formbody": "^8.0.2",
    "fastify": "^5.2.1"
  }
}
