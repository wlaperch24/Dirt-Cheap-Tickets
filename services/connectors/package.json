{
  "name": "@dct/connectors",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "lint": "echo 'lint not configured for @dct/connectors'",
    "test": "echo 'no tests for @dct/connectors'",
    "format": "prettier --write 'src/**/*.ts'"
  },
  "dependencies": {
    "@dct/config": "workspace:*",
    "@dct/contracts": "workspace:*",
    "@dct/db": "workspace:*",
    "@dct/runtime": "workspace:*",
    "bullmq": "^5.43.1",
    "fastify": "^5.2.1"
  }
}
