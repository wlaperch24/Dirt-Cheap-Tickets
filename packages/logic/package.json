{
  "name": "@dct/logic",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "lint": "echo 'lint not configured for @dct/logic'",
    "test": "echo 'no tests for @dct/logic'",
    "format": "prettier --write 'src/**/*.ts'"
  },
  "dependencies": {
    "@dct/contracts": "workspace:*"
  }
}
