{
  "name": "@dct/runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "lint": "echo 'lint not configured for @dct/runtime'",
    "test": "echo 'no tests for @dct/runtime'",
    "format": "prettier --write 'src/**/*.ts'"
  },
  "dependencies": {
    "@dct/config": "workspace:*",
    "bullmq": "^5.43.1",
    "ioredis": "^5.4.2",
    "pino": "^9.6.0"
  }
}
